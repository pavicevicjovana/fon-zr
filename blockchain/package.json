{
  "name": "velura-blockchain",
  "version": "1.0.0",
  "description": "Lokalna privatna blokcejn mreza i SagaAudit pametni ugovor za sistem Velura",
  "private": true,
  "scripts": {
    "node": "hardhat node --hostname 0.0.0.0",
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network docker"
  },
  "devDependencies": {
    "hardhat": "^2.22.0",
    "@nomicfoundation/hardhat-ethers": "^3.0.5",
    "ethers": "^6.13.0"
  }
}